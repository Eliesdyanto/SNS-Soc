filter {
  if [agent][type] == "packetbeat" {
    mutate { add_tag => ["packetbeat"] }
  }
}

output {
  if "packetbeat" in [tags] {
    elasticsearch {
      hosts => ["http://192.168.0.76:9200"]
      index => "packetbeat-%{+YYYY.MM.dd}"
      manage_template => false
    }
  }
}
