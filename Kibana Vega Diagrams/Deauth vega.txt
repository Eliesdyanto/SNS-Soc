{
  "$schema": "https://vega.github.io/schema/vega/v5.json",
  "padding": 0,

  "data": [
    {
      "name": "deauth_check",
      "url": {
        "index": "kismet-alerts-*",
        "body": {
          "size": 0,
          "track_total_hits": true,
          "query": {
            "bool": {
              "filter": [
                {
                  "range": {
                    "@timestamp": {
                      "gte": "now-5m",
                      "lte": "now"
                    }
                  }
                },
                {
                  "term": {
                    "kismet.alert.header.keyword": "DEAUTHFLOOD"
                  }
                }
              ]
            }
          }
        }
      },
      "format": { "property": "hits" },
      "transform": [
        {
          "type": "formula",
          "as": "docCount",
          "expr": "datum.total && datum.total.value != null ? datum.total.value : datum.total"
        },
        {
          "type": "formula",
          "as": "isAlert",
          "expr": "datum.docCount > 0"
        },
        {
          "type": "formula",
          "as": "bgColor",
          "expr": "datum.isAlert ? '#8B0000' : '#006400'"
        },
        {
          "type": "formula",
          "as": "statusText",
          "expr": "datum.isAlert ? 'DEAUTH ATTACK DETECTED' : 'NO DEAUTH ATTACK'"
        }
      ]
    }
  ],

  "marks": [
    {
      "type": "rect",
      "from": { "data": "deauth_check" },
      "encode": {
        "enter": {
          "href": { "value": "http://localhost:2501/" },
          "cursor": { "value": "pointer" }
        },
        "update": {
          "x": { "value": 0 },
          "y": { "value": 0 },
          "x2": { "signal": "width" },
          "y2": { "signal": "height" },
          "fill": { "field": "bgColor" }
        },
        "hover": {
          "fillOpacity": { "value": 0.8 }
        }
      }
    },
    {
      "type": "text",
      "from": { "data": "deauth_check" },
      "encode": {
        "enter": {
          "href": { "value": "http://localhost:2501/" },
          "cursor": { "value": "pointer" }
        },
        "update": {
          "x": { "signal": "width / 2" },
          "y": { "signal": "height / 2 - 20" },
          "align": { "value": "center" },
          "baseline": { "value": "middle" },
          "fontSize": {
            "signal": "clamp(min(width, height) / 8, 18, 100)"
          },
          "fontWeight": { "value": "bold" },
          "fill": { "value": "white" },
          "text": { "field": "statusText" }
        }
      }
    },
    {
      "type": "text",
      "from": { "data": "deauth_check" },
      "encode": {
        "enter": {
          "href": { "value": "http://localhost:2501/" },
          "cursor": { "value": "pointer" }
        },
        "update": {
          "x": { "signal": "width / 2" },
          "y": { "signal": "height / 2 + 45" },
          "align": { "value": "center" },
          "baseline": { "value": "middle" },
          "fontSize": {
            "signal": "clamp(min(width, height) / 14, 14, 64)"
          },
          "fill": { "value": "white" },
          "text": {
            "signal": "'Alerts in last 5 min: ' + datum.docCount"
          }
        }
      }
    }
  ]
}